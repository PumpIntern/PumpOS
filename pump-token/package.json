{
  "name": "pump-token",
  "version": "1.0.0",
  "description": "pumpOS",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "clean": "npx hardhat clean",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network ",
    "verify": "npx hardhat run scripts/verify.js --network ethereum"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.7",
    "node-fetch": "^2.7.0"
  }
}
